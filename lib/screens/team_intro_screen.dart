import 'package:flutter/material.dart';

class TeamIntroScreen extends StatelessWidget {
  const TeamIntroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Text('Team Intro screen'),
    );
  }
}